# AquaWatch
.venv/
__pycache__/
*.pyc
.DS_Store

# Raw geodata downloads (re-fetchable; processed/*.geojson stays committed)
data/raw/*
!data/raw/.gitkeep
*.tif
*.tiff
*.zip

# DEM / tile imagery is heavy and re-fetchable — but keep committed docs diagrams
*.png
!docs/*.png
